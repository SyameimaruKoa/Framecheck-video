# 旧ゲーム機向け HTTPサーバー 一時公開ガイド

このガイドでは、モニター残像・フレーム測定用動画を PSP, Wii U, 3DS などの古いゲーム機（暗号化 HTTPS 通信や最新 TLS 規格に対応していないレガシー機器）からローカルネットワーク経由で閲覧するために、一時的な HTTP サーバーを公開する手順を説明します。

---

## 0. 重要：旧ゲーム機からの接続ルール

- **暗号化なしの「HTTP」で接続してください**：
  古いゲーム機の内蔵ブラウザは、安全な通信（HTTPS / SSL）に必要な暗号化アルゴリズムや最新のルート証明書に対応しておらず、HTTPSでアクセスすると接続エラー（通信エラー、セキュリティエラー）になります。必ず `http://[サーバーのIPアドレス]:24680/` という形で、**HTTP** でアクセスしてください。
- **同じWi-Fi（ローカルネットワーク）に接続する**：
  サーバーとなるPC（またはスマートフォン）と、アクセスするゲーム機は、必ず同じWi-Fiルーター（同一LAN内）に接続してください。
- **ゲーム機別の再生対応状況**：
  - **Wii U / 3DS**: **Flash（SWF）の再生には一切対応していません（ブラウザが非対応です）**。Wii U等で再生する場合は、トップページから動画（MP4）を再生するか、JS非対応向けの「[通常動画版 (サーバー側倍速計算)](/nojs)」をご利用ください。
  - **PSP**: 内蔵の Flash Player による `.swf` ファイルの再生に対応しています。

---

## ※ レガシー機器向けの自動互換エンコードについて
古いゲーム機の内蔵デコーダーは、高解像度（1080pや4K）や高度なエンコードプロファイル（H.264 High Profile / YUV444p等）をデコードできず、再生エラーになります。
本サーバー（Dockerまたはnpm起動時）は、**アクセス元のゲーム機（Wii U, PSP等）のユーザーエージェントを自動検出・判定し、1.0倍速であっても自動的にデコーダ制限（Wii Uは720p、PSPは480px）および互換プロファイル（H.264 Main Profile / yuv420p）にサーバー側でリアルタイム変換（トランスコード）してキャッシュ配信**します。これによりデコードエラーを回避して動画が再生可能になります。

---

## ※ PSP での Flash 再生時に「メモリ不足」と表示される場合の対策
PSP の内蔵ウェブブラウザは割り当てられているメモリ容量が極めて小さいため（実質 3〜4MB 程度）、HTML埋め込みのプレイヤー画面を経由して SWF を読み込もうとすると、HTML自体のメモリ消費が上乗せされて高確率で「メモリ不足」エラーとなり起動できません。

メモリ不足エラーを回避するため、以下の対策を行ってください：

1. **「SWFを直接再生」リンクを利用する (推奨)**
   「ファイル一覧」や「JS非対応ブラウザ向けページ」に表示されている **`SWFを直接再生 (PSP等のメモリ不足対策)`** という直リンク（SWFファイルそのもののURL）をクリックしてください。HTMLのオーバーヘッド（iframeやテキスト描画等）が無くなり、ブラウザの消費メモリが最小限に抑えられるため、PSPでも正常に Flash を起動しやすくなります。
2. **PSP ブラウザのメモリ節約設定を行う**
   PSP のブラウザ側で以下の設定を行い、空きメモリ領域を増やしてください：
   - ブラウザメニューの **「ツール」** ＞ **「設定」** ＞ **「表示設定」** を開き、**「画像の表示」を「オフ」**にします。
   - **「ツール」** ＞ **「設定」** ＞ **「キャッシュ設定」** を開き、キャッシュサイズを **「使用しない」** にするか、キャッシュの消去を行います。
   - **「ツール」** ＞ **「Cookie消去」** および **「入力履歴消去」** を実行し、ブラウザの不要な履歴データをクリアします。

---

## 1. 各OSにおけるローカルIPアドレスの確認方法

ゲーム機からアクセスする際、サーバーPCの「ローカルIPアドレス（IPv4）」が必要です。以下の方法で確認してください。

### Windows
1. スタートボタンを右クリックし、「ターミナル」または「PowerShell」または「コマンドプロンプト」を開きます。
2. 以下のコマンドを実行します：
   ```cmd
   ipconfig
   ```
3. 接続中のネットワーク（「無線 LAN アダプター Wi-Fi」など）の出力結果から、**「IPv4 アドレス」**の値（例: `192.168.1.15` または `192.168.11.5`）を確認し、控えます。

### Ubuntu (Linux)
1. 端末（ターミナル）を開きます。
2. 以下のいずれかのコマンドを実行します：
   ```bash
   hostname -I
   ```
   または
   ```bash
   ip a
   ```
3. 表示されるIPアドレス（例: `192.168.1.20`）を確認し、控えます。

### Android
- **設定アプリから確認する場合**:
  「設定」＞「ネットワークとインターネット」＞「Wi-Fi」＞ 接続中のWi-Fi名の横の歯車マーク ＞「詳細設定」から「IPアドレス」を確認します。
- **コマンド (Termux アプリ等) から確認する場合**:
  Termuxアプリで以下のコマンドを実行して確認します：
  ```bash
  ip route
  ```

### iOS (iPhone/iPad)
1. 「設定」アプリを開きます。
2. 「Wi-Fi」をタップし、接続中のネットワークの右側にある「i」マークをタップします。
3. 下部にある「IPv4 アドレス」の欄の値を確認し、控えます。

---

## 2. サーバーの公開手段 (複数方式)

環境やインストールされているソフトに合わせて、以下のいずれかの方法でサーバーを起動してください。

### 方式A. Docker Compose 方式 (最も推奨)
ホストPCにNode.jsやFFmpegを別途インストールする必要がなく、JS非対応ブラウザ向けの「サーバー側倍速動画計算」も完全に動作します。
1. Docker および Docker Compose (Docker Desktop) がインストールされていることを確認します。
2. プロジェクトのルートディレクトリで以下のコマンドを実行します：
   ```bash
   docker-compose up
   ```
3. これにより、**24680 番ポート**で HTTP サーバーが立ち上がります。
4. ゲーム機から `http://[PCのIPアドレス]:24680/` にアクセスします。

### 方式B. npm (Node.js) 方式
ホストPCにNode.jsがインストールされている場合に使用します。

> [!WARNING]
> **Wii U や PSP などの古いゲーム機へ動画を配信する場合は、ホストPCに `ffmpeg` のインストールが必須です。**
> ホストPCに `ffmpeg` がインストールされていない場合、Wii U 等のデコーダ制限に合わせた互換エンコード（Main Profile への変換）が実行できません。トランスコードに失敗した場合は元の動画ファイルをそのまま配信しますが、元の動画ファイルの形式（プロファイルや解像度など）がゲーム機に合っていないと、ゲーム機側で「読み込みに失敗しました」と表示されて再生できません。
> （※Docker 方式であれば、コンテナ内に自動的に `ffmpeg` が同梱されるため、PCへの個別インストールは不要です）

1. プロジェクトルートで以下を実行して依存パッケージをインストールします：
   ```bash
   npm install
   ```
2. サーバーを起動します：
   ```bash
   npm start
   ```
3. サーバーは **24680 番ポート**で起動します。
4. ゲーム機から `http://[PCのIPアドレス]:24680/` にアクセスします。

### 方式C. Python 簡易サーバー方式 (静的ファイル配信のみ)
Pythonがインストールされている環境であれば、最も手軽に起動できます（映像の倍速計算機能は利用できません）。
- **Python 3 以上**:
  ```bash
  python -m http.server 24680
  ```
- **Python 2 (古い環境)**:
  ```bash
  python -m SimpleHTTPServer 24680
  ```
- ゲーム機から `http://[PCのIPアドレス]:24680/` にアクセスします。

### 方式D. PHP 内蔵サーバー方式 (静的ファイル配信のみ)
PHPがインストールされている環境で使用できます（映像の倍速計算機能は利用できません）。
```bash
php -S 0.0.0.0:24680
```
- ゲーム機から `http://[PCのIPアドレス]:24680/` にアクセスします。

---

## 3. スマートフォン・タブレットでの簡易公開方法

PCを使わず、スマートフォン自身をサーバーにしてゲーム機からアクセスさせることも可能です。

### Android での簡易公開
1. Google Play から 「Simple HTTP Server」 や 「Termux」 などのアプリをインストールします。
2. **Simple HTTP Server アプリを使用する場合**:
   - アプリを開き、公開するフォルダとして本プロジェクトフォルダ（または動画ファイルがある場所）を選択します。
   - ポート番号（例: `24680`）を設定し、サーバーをONにします。
3. **Termux アプリを使用する場合**:
   - `pkg install python` を実行して Python をインストールします。
   - プロジェクトフォルダに移動して `python -m http.server 24680` を実行します。

### iOS での簡易公開
1. App Store から 「iSH Shell」 (Linuxエミュレータ) や 「Local HTTPServer」 などのWebサーバーアプリをインストールします。
2. **iSH Shell を使用する場合**:
   - `apk add python3` を実行して Python をインストールします。
   - ファイル共有機能等でプロジェクトフォルダをマウント、または動画ファイルをコピーし、`python3 -m http.server 24680` を実行します。

---

## 4. 起動スクリプトの利用方法

本プロジェクトには、サーバー起動を簡略化するための対話式スクリプトが含まれています。
- **Windows**: `start-server.bat` を実行します。
- **Linux / Ubuntu**: `chmod +x start-server.sh` で実行権限を付与し、`./start-server.sh` を実行します。

※それぞれ `-h` または `--help` オプションを付けて起動することで、詳細な使い方のヘルプメッセージを表示します。
