{
  "name": "framecheck-video-server",
  "version": "1.0.0",
  "description": "Local server for framecheck-video with server-side speed computations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
